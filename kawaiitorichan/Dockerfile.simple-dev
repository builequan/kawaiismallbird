FROM node:18-alpine

WORKDIR /app

# Copy everything
COPY . .

# Install dependencies with npm (more reliable)
RUN npm install --legacy-peer-deps

# Expose port
EXPOSE 3000

# Environment
ENV HOSTNAME=0.0.0.0

# Run with npm instead of pnpm
CMD ["npm", "run", "dev"]