FROM node:18-alpine

WORKDIR /app

# Copy everything
COPY . .

# Install dependencies
RUN npm install --legacy-peer-deps

# Expose port directly
EXPOSE 80

# Environment - run on port 80
ENV HOSTNAME=0.0.0.0
ENV PORT=80

# Run dev server on port 80
CMD ["npm", "run", "dev"]